apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphiti-mcp
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: graphiti-mcp
  template:
    metadata:
      labels:
        app: graphiti-mcp
    spec:
      containers:
        - name: graphiti-mcp
          image: zepai/knowledge-graph-mcp:standalone
          command:
            - /root/.local/bin/uv
            - run
            - --no-sync
            - main.py
          env:
            - name: FALKORDB_URI
              value: redis://falkordb.graphiti.svc.cluster.local:6379
            - name: FALKORDB_DATABASE
              value: main
            - name: GRAPHITI_GROUP_ID
              value: main
            - name: SEMAPHORE_LIMIT
              value: "2"
            - name: CONFIG_PATH
              value: /app/mcp/config/config.yaml
            - name: OPENAI_API_KEY
              value: no-key
            - name: OPENAI_BASE_URL
              value: http://llamacpp-graphiti-svc-proxy.llms.svc.cluster.local:8080/v1
            - name: LLM__MODEL
              value: qwen3-4b-2507
            - name: OPENAI_API_URL
              value: http://llamacpp-embed-svc-proxy.llms.svc.cluster.local:8080/v1
            - name: EMBEDDER__MODEL
              value: nomic-embed
            - name: EMBEDDER__DIMENSIONS
              value: "768"
            - name: GRAPHITI_TELEMETRY_ENABLED
              value: "false"
          ports:
            - containerPort: 8000
              name: http
          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 1Gi
              cpu: 250m
            limits:
              memory: 4Gi
              cpu: 2
          volumeMounts:
            - name: factories-patch
              mountPath: /app/mcp/src/services/factories.py
              subPath: factories.py
            - name: mcp-server-patch
              mountPath: /app/mcp/src/graphiti_mcp_server.py
              subPath: graphiti_mcp_server.py
      volumes:
        - name: factories-patch
          configMap:
            name: graphiti-factories
        - name: mcp-server-patch
          configMap:
            name: graphiti-mcp-server
---
apiVersion: v1
kind: Service
metadata:
  name: graphiti-mcp
spec:
  selector:
    app: graphiti-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: graphiti
  annotations:
    tailscale.com/proxy-class: "tailscale-small"
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: graphiti-mcp
                port:
                  number: 8000
  tls:
    - hosts:
        - graphiti
